# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.venv/
venv/

# uv: this is a library with deliberately loose pins, tested across a Python
# matrix, so the resolution is not pinned and the interpreter is not forced.
uv.lock
.python-version

# Tooling caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
htmlcov/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# direnv (may hold a MERMAID_API_TOKEN)
.envrc
