<!--
The guide is the repository README, inlined by pymdownx.snippets rather than
copied: tests/test_docs.py parses README.md and checks its migration table and
`get_project_data()` matrix against the package, so a second copy here would be
the one that drifts.  Edit README.md, not this file.
-->

--8<-- "README.md"
