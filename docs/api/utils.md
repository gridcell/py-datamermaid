# datamermaid.utils

Internal helpers, documented because they define the shape of every frame the
package returns: records become a `DataFrame` with stable columns, and list
columns collapse to comma-joined strings the way mermaidr does it.

::: datamermaid.utils
