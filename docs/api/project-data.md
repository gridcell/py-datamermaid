# datamermaid.project_data

Survey data, addressed by sampling `method` and aggregation `level`.
[`construct_endpoints()`][datamermaid.project_data.construct_endpoints] is the
matrix of the CSV endpoints those two arguments select, and it is the same
matrix the guide tabulates.

::: datamermaid.project_data
