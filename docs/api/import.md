# datamermaid.import_

The write path. Every function here that changes something in MERMAID takes an
explicit `confirm=True`, because nothing prompts and these are meant to be
runnable unattended.

::: datamermaid.import_
