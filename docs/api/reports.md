# datamermaid.reports

Reports MERMAID generates on request. The GFCR report is an Excel workbook, so
[`get_gfcr_report()`][datamermaid.reports.get_gfcr_report] returns one
`DataFrame` per worksheet and needs the `datamermaid[excel]` extra.

::: datamermaid.reports
